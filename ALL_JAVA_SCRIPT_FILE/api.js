// const host = 'http://localhost:8000';
const host = 'http://192.168.90.108:8000';

const admin = `${host}/admin`;
const adminLogin = `${host}/admin/login`;
const doctors = `${host}/doctors`;
const patients = `${host}/patients`;
const employees  = `${host}/employees`;
const employeeslogin  = `${host}/employees/login`;
const appoinments = `${host}/appoinments`;
const users = `${host}/users`;